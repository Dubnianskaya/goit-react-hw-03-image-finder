import React from 'react';
import { Overlay, ModalWindow } from './Modal.styled';

const Modal = () => (
  <Overlay>
    <ModalWindow>
      <img src="" alt="" />
    </ModalWindow>
  </Overlay>
);

export default Modal;
